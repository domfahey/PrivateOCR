// Settings page logic
document.addEventListener("DOMContentLoaded", () => {
  // Future: Load settings from storage and populate fields
  // Currently, this page is static/informational as most settings are hardcoded/defaults
  console.log("Settings page loaded");
});
